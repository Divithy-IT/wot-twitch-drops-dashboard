import{useState}from'react';import{Play,ExternalLink}from'lucide-react';
export default function Watching(){const[channel,setChannel]=useState('worldoftanks'),[playing,setPlaying]=useState(false);const safe=channel.replace(/[^a-zA-Z0-9_]/g,'');return <><div className="pagehead"><div><span className="eyebrow">PRAWDZIWA SESJA TWITCH</span><h1>Oglądanie</h1><p>Oficjalny odtwarzacz Twitch Embed. Oglądanie uruchamiasz samodzielnie.</p></div></div><div className="watch-controls"><input value={channel} onChange={e=>setChannel(e.target.value)} placeholder="Nazwa kanału"/><button onClick={()=>setPlaying(true)}><Play/>Uruchom stream</button><a className="button secondary" target="_blank" href={`https://twitch.tv/${safe}`}>Nowa karta <ExternalLink/></a></div>{playing&&safe?<div className="player"><iframe title="Twitch" src={`https://player.twitch.tv/?channel=${safe}&parent=${location.hostname}&autoplay=false`} allowFullScreen/></div>:<div className="empty tall">Wybierz kanał i świadomie uruchom odtwarzanie.</div>}<div className="limitation">Ta aplikacja nie symuluje oglądania, nie wysyła heartbeatów i nie omija reklam. Twitch sam decyduje, czy dana sesja kwalifikuje się do Drops.</div></>}

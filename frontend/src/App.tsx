import{useEffect,useState}from'react';import{Routes,Route,Navigate,NavLink}from'react-router-dom';import{LayoutDashboard,Settings,Radio,Trophy,LogOut}from'lucide-react';import{api}from'./api';import Dashboard from'./pages/Dashboard';import Campaigns from'./pages/Campaigns';import Watching from'./pages/Watching';import SettingsPage from'./pages/Settings';import Login from'./pages/Login';
type Auth={authenticated:boolean;setup_required:boolean;username?:string};
export default function App(){const[a,setA]=useState<Auth|null>(null);useEffect(()=>{api<Auth>('/auth/state').then(setA).catch(()=>setA({authenticated:false,setup_required:false}))},[]);if(!a)return <div className="splash">Uruchamianie centrum dowodzenia…</div>;if(!a.authenticated)return <Login setup={a.setup_required} done={()=>location.reload()}/>;return <div className="shell"><aside><div className="brand"><span>WOT</span><strong>DROPS COMMAND</strong></div><nav><NavLink to="/"><LayoutDashboard/>Pulpit</NavLink><NavLink to="/campaigns"><Trophy/>Kampanie</NavLink><NavLink to="/watching"><Radio/>Oglądanie</NavLink><NavLink to="/settings"><Settings/>Ustawienia</NavLink></nav><button className="logout" onClick={()=>api('/auth/logout',{method:'POST'}).then(()=>location.reload())}><LogOut/>Wyloguj</button></aside><main><Routes><Route path="/" element={<Dashboard/>}/><Route path="/campaigns" element={<Campaigns/>}/><Route path="/watching" element={<Watching/>}/><Route path="/settings" element={<SettingsPage/>}/><Route path="*" element={<Navigate to="/"/>}/></Routes></main></div>}
